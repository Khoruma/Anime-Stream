part of 'pages.dart';

class RelasePage extends StatelessWidget {
  const RelasePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Release Page'),);
  }
}